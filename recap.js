let firstName = "John" // string
const age = 20 // number
let bdate = new Date() // date
let isWorking = true // boolean
let partner = null // null (value of nothing)
let pet; // undefined (nothing)
const sum = 5 + 1000
const greeting = "My name is " + firstName + ". I am " + age + " years old." // old ancient avoid
const newGreeting = `My name is ${firstName}. I am ${age} years old.` // cool new fresh hip
